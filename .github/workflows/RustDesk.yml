name: Windows - RustDesk

on:
  workflow_dispatch:

jobs:
  build:
    name: Start Building...
    runs-on: windows-latest

    steps:
      - name: Downloading & Installing Essentials
        run: |
          Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/4oe8lp8iid6bjbsmy0hd2/Downloads.bat?rlkey=0xvgj5nvngbqq614slhwn5ww2&dl=1" -OutFile "Downloads.bat"
          cmd /c Downloads.bat

      - name: Log in to AnyDesk
        run: cmd /c show.bat

